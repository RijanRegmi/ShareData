# def take(self):
    # con=Nonesql="""SELECT* FROM login WHERE contact=%s AND password=%s"""
    # value=(self.contact.get(), self.password())
    # result=None
    # try:
    #     conn=Connect()
    #     cursor=conn.cursor()
    #     cursor.exicute(sql,values)
    #     result=cursor.fetchone()
    #     cursor.close()

    # expect:
    #     print("Error",sys.exe_info())
    
    # finally:
    #     del value, sql, conn
    # return result
    
